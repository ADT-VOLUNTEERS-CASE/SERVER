services:
  postgres:
    image: postgres:17-alpine
    container_name: volunteers_case_db_dev
    environment:
      POSTGRES_USER: ${DB_USRNAME:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
      POSTGRES_DB: ${DB_NAME:-volunteers_case}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data_dev:/var/lib/postgresql/data
    networks:
      - volunteers_network_dev
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USRNAME:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: volunteers_case_app_dev
    environment:
      DB_URL: jdbc:postgresql://postgres:5432/${DB_NAME:-volunteers_case}
      DB_USRNAME: ${DB_USRNAME:-postgres}
      DB_PASSWORD: ${DB_PASSWORD:-postgres}
      SECRET_KEY: ${SECRET_KEY}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD}
      COORDINATOR_PASSWORD: ${COORDINATOR_PASSWORD}
      BASE_USER_PASSWORD: ${BASE_USER_PASSWORD}
      BASE_USER_TWO_PASSWORD: ${BASE_USER_TWO_PASSWORD}
      BASE_USER_THREE_PASSWORD: ${BASE_USER_THREE_PASSWORD}
      S3_ENDPOINT: ${S3_ENDPOINT}
      S3_REGION: ${S3_REGION}
      S3_BUCKET: ${S3_BUCKET}
      S3_ACCESS_KEY: ${S3_ACCESS_KEY}
      S3_SECRET_KEY: ${S3_SECRET_KEY}
      S3_PUBLIC_BASE_URL: ${S3_PUBLIC_BASE_URL}
      S3_PATH_STYLE_ACCESS: ${S3_PATH_STYLE_ACCESS:-false}
      S3_COVER_PREFIX: ${S3_COVER_PREFIX:-covers}
      SPRING_PROFILES_ACTIVE: dev
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - volunteers_network_dev

volumes:
  postgres_data_dev:

networks:
  volunteers_network_dev:
    driver: bridge
